pluginManagement {
    includeBuild('../grimoire')  // adjust path to sibling folder

    repositories {
        gradlePluginPortal()
        mavenCentral()
        mavenLocal()
    }
}

rootProject.name = 'xgeoff'
