pluginManagement {
    includeBuild('grimoire')  // use vendored plugin

    repositories {
        gradlePluginPortal()
        mavenCentral()
        mavenLocal()
    }
}

rootProject.name = 'xgeoff'
